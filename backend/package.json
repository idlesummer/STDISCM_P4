{
  "name": "backend",
  "version": "0.1.0",
  "private": true,
  "description": "Python gRPC training server",
  "scripts": {
    "start": "uv run python -m src.main",
    "dev": "uv run python -m src.main",
    "venv": "cmd.exe /K .venv\\Scripts\\activate",
    "subscribe": "uv run python -m src.subscriber",
    "install": "uv sync"
  }
}
